# 층 공략 — 하나의 층, 여러 개의 답

## 원칙

**모든 층은 최소 세 가지 방법으로 통과할 수 있다.**

1. **돌파** — 전투력으로 찍어누른다. 항상 존재한다
2. **열쇠** — 특정 조건을 갖추면 전투력이 낮아도 통과한다
3. **묘수** — 더 어려운 조건. 최상급 보상

플레이어마다 로스터가 다르므로(`05-고유성.md`), 조건은 **특정 캐릭터가 아니라 속성**으로 건다.
무명 잔상도 생업·시대·역할을 가지므로 똑같이 열쇠가 될 수 있다.

> 돌파 경로가 항상 있어야 한다. 조건을 못 맞춘 플레이어가 **영구히 막히면 안 된다.**
> 대신 돌파는 비효율적이어야 한다 — 시간이 훨씬 오래 걸린다.

## 조건의 종류

| 종류 | 예시 | 의도 |
|---|---|---|
| **역할** | `수호` 2명 이상 | 조합 사고 유도 |
| **생업** | `담장` 또는 `대장간` 보유 | 뜰 시스템과 전투를 연결 |
| **시대** | 해당 층 출신 1명 이상 | *그 자리에 있었던 사람*의 가치 |
| **소수** | 3명 이하로 클리어 | 고성능 소수 정예 |
| **약체** | 평균 선명도 3 이하 | **약한 캐릭터로 푸는 길** |
| **기억** | 본디 도달자 1명 포함 | 장기 육성 보상 |

`약체` 조건이 이 게임에서 특히 중요하다.
★1~★2만으로 클리어하면 보상이 가장 크다. 저등급 잔상이 버려지지 않는다.

## 보상 차등

| 경로 | 보상 |
|---|---|
| 돌파 | 기본 |
| 열쇠 1개 | 기본 + 유품 |
| 열쇠 2개 이상 | 기본 + 유품 + **기억 조각** |
| 약체/소수 | **해당 층 무명 잔상 발견 확률 대폭 상승** |

핵심: 조건 클리어의 보상을 **전투력**이 아니라 **새로운 사람과의 만남**으로 준다.
그래야 "더 세지려고 푼다"가 아니라 "더 만나려고 푼다"가 된다.

## 예시 — 24층 「닫힌 성문」

붕괴기. 북문이 닫혀 있고, 문 바깥에 사람들이 있다.

| 경로 | 조건 | 서사 |
|---|---|---|
| 돌파 | 전투력 100% | 문을 부순다 |
| 열쇠 A | `수호` 2명 이상 · 전투력 60% | 둘이 문을 지탱하는 동안 통과 |
| 열쇠 B | 24층 출신 1명 · 전투력 45% | **배수로를 아는 사람이 있다** |
| 열쇠 C | 생업 `담장` 보유 · 전투력 55% | 담의 약한 지점을 안다 |
| 묘수 | 열쇠 B + 약체 · 전투력 30% | 이름 없는 사람들이 이름 없는 길로 빠져나간다 |

열쇠 B에서 `북문의 수문장`(★3)이 `카브릴`(★5)보다 유용하다.
앞서 정한 **"저등급 잔상은 자기가 죽은 층에서 가장 강하다"** 가 여기서 실제 규칙이 된다.

묘수 경로의 서사가 이 게임의 주제 그 자체다.

## 방치형과의 결합

방치 전투는 자동이지만, **편성은 플레이어가 한다.**

- 자동 진행은 `돌파` 경로만 시도한다 → 느리지만 언젠가 뚫린다
- 열쇠 경로는 플레이어가 직접 편성해야 발동한다
- 즉 **접속하면 훨씬 빨리 오른다.** 접속하지 않아도 막히지는 않는다

방치형의 편의성과 퍼즐의 능동성이 충돌하지 않는 지점이다.

## 데이터 형식

```json
{
  "floor": 24, "era": "붕괴기", "name": "닫힌 성문",
  "base_power": 100,
  "routes": [
    { "id": "force", "power": 1.00, "conditions": [] },
    { "id": "guard", "power": 0.60, "conditions": [{"type":"역할","value":"수호","count":2}] },
    { "id": "drain", "power": 0.45, "conditions": [{"type":"시대","value":24,"count":1}] },
    { "id": "wall",  "power": 0.55, "conditions": [{"type":"생업","value":"담장","count":1}] },
    { "id": "unseen","power": 0.30,
      "conditions": [{"type":"시대","value":24,"count":1},{"type":"약체","value":3}],
      "reward_tier": "묘수" }
  ]
}
```

조건이 전부 데이터라서 층을 100개 만들어도 코드 수정이 필요 없다.


---

## 2막 확장 검증 (31~60층)

`tools/gen_floors.py` · 층 60개 생성

| 구분 | 수 |
|---|---|
| 연출 (전투 없음) | 4 |
| 일반 (1팀 5명) | 46 |
| 연계 (2팀 10명) | 5 — 15·25·35·45·55층 |
| 관문 (3팀 15명) | 5 — 20·30·40·50·60층 |
| 경로가 2개 이상인 층 | 21 |

### 순환 참조 검사

**그 층에서 얻는 캐릭터를 그 층의 열쇠로 요구하면 첫 클리어가 불가능하다.**
전승 캐릭터만으로 열 수 있는지 전 층을 검사해 17건을 잡았고, 3건까지 줄였다.

| 문제 | 조치 |
|---|---|
| 조건 타입 오분류 | `약체` 가 `역할:약체` 로 저장되던 버그. 독립 타입으로 분리 |
| 5층 「수호×1」 | 수호 첫 획득이 5층 자신. 열쇠 제거 (돌파만) |
| 15·20·24층 「수호×2」 | 24층 전까지 전승 수호가 1명뿐. ×1 로 완화 |
| 시대 조건 | **「정확히 N층 출신」은 확률 5%.** 무명 잔상은 시대 범위로 층이 배정되므로 「해당 시대 출신」으로 완화 |
| 빵을 굽던 사람 | 데이터 18층 ↔ 대본 13층 불일치. 13층으로 수정 |

### 남은 3건 — 의도된 것

| 층 | 열쇠 | 판단 |
|---|---|---|
| 10층 | 약체 3명 | 10층 시점 보유 8명이라 뽑기로 충족된다 |
| 41층 | 생업 `종탑` | **세렌의 종탑.** 무명 잔상 의존이 맞다 |
| 53층 | 생업 `악기` | **축제.** 특별해야 하는 열쇠다 |

돌파 경로는 전 층에 있으므로 막히지 않는다. 위 열쇠들은 **보너스**다.
